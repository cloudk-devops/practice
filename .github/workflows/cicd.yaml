on:
  workflow_dispatch:
    inputs:
      environment:
        required: true
        type: choice
        options:
          - dev
          - prod
jobs:
  print_hello:
    runs-on: ubuntu-latest
    steps:
      - run: 
          echo "This is:${{ inputs.environment }}"


