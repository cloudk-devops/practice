on:
  workflow_dispatch:
    inputs:
      environment:
        required: true
        type: choice
        default: 'QA'
        options:
          - dev
          - prod
jobs:
  print_env:
    runs-on: ubuntu-latest
    steps:
      - run: 
          echo "This is:${{ inputs.environment }}"


