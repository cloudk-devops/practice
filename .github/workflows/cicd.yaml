name: Test
on: 
  workflow_dispatch:
env:
  Name: "Muneer"
jobs:
  condition:
    runs-on: ubuntu-latest
    steps:
      - name: if feature
        # if: github.ref == 'refs/heads/feature-**'
        run: echo "Name is $Name"
      

        
