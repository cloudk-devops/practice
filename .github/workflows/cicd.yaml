on:
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
inputs:
  name:
    required: true
    type: string
  age:
    required: false
    type: number
jobs:
  print_hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello..Muneer"


