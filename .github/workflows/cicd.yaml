name: Test
on: 
  workflow_dispatch:
jobs:
  condition:
    runs-on: ubuntu-latest
    steps:
      - name: if feature
        if: github.ref == 'refs/heads/main'
        run: echo " Main brach Bro"

        
