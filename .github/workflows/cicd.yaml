on:
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  print_hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello..Muneer"
      # - run: sleep 1000
  sleep:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 10


